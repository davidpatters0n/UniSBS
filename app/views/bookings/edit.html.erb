
<%= form_for(@booking) do |f| %>
  <%= render :partial => 'form', :locals=>{:f=>f} %>
  or <%= link_to_booking_in_diary 'Show in diary', @booking %> |
  <%= link_to 'List Bookings', bookings_path %>
<% end %>

